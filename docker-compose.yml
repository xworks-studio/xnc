services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: xnc
      POSTGRES_PASSWORD: xnc
      POSTGRES_DB: xnc
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes:
  pgdata:
